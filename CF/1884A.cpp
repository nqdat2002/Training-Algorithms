#include <bits/stdc++.h>

using namespace std;

const int N = 1e5 + 5;
int t, f[N];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> t;
    while(t--) {
        int a, b; cin >> a, b;
        cout << a + b << endl;
    }
}
